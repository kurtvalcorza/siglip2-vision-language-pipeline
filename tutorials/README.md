# Tutorials

[![GitHub](https://img.shields.io/badge/GitHub-181717?style=flat&logo=github&logoColor=white)](https://github.com/kurtvalcorza/siglip2-vision-language-pipeline)
[![Open In Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/kurtvalcorza/siglip2-vision-language-pipeline/blob/main/tutorials/siglip2_vision_language_colab.ipynb)
[![Hugging Face](https://img.shields.io/badge/%F0%9F%A4%97%20Hugging%20Face-google%2Fsiglip2--base--patch16--224-ffcc4d?style=flat)](https://huggingface.co/google/siglip2-base-patch16-224)
[![Upstream](https://img.shields.io/badge/Upstream-huggingface%2Ftransformers-181717?style=flat&logo=github&logoColor=white)](https://github.com/huggingface/transformers)
[![arXiv](https://img.shields.io/badge/arXiv-2502.14786-b31b1b.svg)](https://arxiv.org/abs/2502.14786)

Notebook specification: **DIMER Notebook Specification 2.0** — the notebook is **standalone** (§4) and declares its profile and pedagogical mode (§3.4): it is
generated by `tools/build_notebook.py` from `tools/notebook_template.py` and carries the repository's package
(six modules: `config.py`, `model.py`, `metrics.py`, `samples.py`, `pipeline.py`, `provenance.py`), the model
identity, the snapshot manifest and the runtime pins, so the exported `.ipynb` keeps working without this repository
being reachable. Do not edit the notebook by hand; edit the package or the template and regenerate
(`python tools/build_notebook.py`; `--check` is enforced by the validator and CI).

| Notebook | Profile | Mode | Carrier | Capability | Default runtime | Sample | BYOD | Run-all | Release status |
|---|---|---|---|---|---|---|---|---|---|
| `siglip2_vision_language_colab.ipynb` | `E2E` | `GUIDED` | standalone (generated) | zero-shot image classification, image/text embeddings, cosine similarity, text-to-image retrieval and bounded supervised fine-tuning with `google/siglip2-base-patch16-224` (Apache-2.0; SigLIP 2 ViT-B/16 at 224 px) — the safetensors snapshot digest-verified against the inline manifest before loading; the inference contract with its input manifest, rejection probe and uncalibrated-sigmoid semantics on three drawn shapes; a digest-pinned real photograph corpus (360 CC0 iNaturalist photographs of six bird species, each pinned by byte size and SHA-256) validated and split per species without leakage; accuracy, macro F1, per-species recall and text-to-image mAP (numpy implementations in the carried `metrics.py`) beside the majority-floor and colour-nearest-neighbour baselines and a second, scientific-name prompt set; bounded fine-tuning of the vision tower's last two blocks, post-layernorm and attention-pool head with SigLIP's sigmoid loss and validation-mAP epoch selection over prompt features embedded once by the frozen text tower; held-out evaluation with a four-way comparison and the per-species breakdown; the drawn shapes re-scored by the adapted model; safetensors adapter export with verified reload parity | CPU (CUDA used automatically when present); float32 | 216 / 48 / 96 iNaturalist photographs (36 / 8 / 16 per species) fetched from the iNaturalist open-data bucket at run time (~39 MB, CC0 1.0) | one zip of images plus `labels.csv` (`id`, `file`, `label`; gated off by default) | ~3 min on the build workstation's CPU after the 1.5 GB checkpoint and the photographs download (longer on a 2-vCPU hosted runtime); ~1.5 min on an RTX 5070 Ti | **Release-grade** — clean-runtime Kaggle Tesla T4 execution of `f3dd43e` / `15f946ab` recorded 2026-09-20 in `../docs/release-verification.md` |

## What changed from the inference-only tutorial

The previous notebook (`MULTI-CAPABILITY`) exercised the five inference operations on three drawn shapes and reported `sample-sanity` metrics on them. This notebook keeps those shapes and their inference contract as Section 5 and adds the adaptation contract around it: a real labelled photograph corpus fetched with per-file pins, a classification and retrieval protocol in the carried `metrics.py` (accuracy, macro F1, per-class recall / precision / F1 / AP, text-to-image mAP), two non-neural baselines, a bounded fine-tuning of the vision tower with the model's own sigmoid-scaled logits and SigLIP's pairwise sigmoid loss, a per-species breakdown on two prompt sets, the shapes re-scored after adaptation, and an artifact that reloads. The per-grid `evaluation_report` helper is unchanged; `pipe.evaluate` is the corpus-level stage (`measured` / `measured-small-sample`).

## Conformance notes

- **Standalone carrier (ST1–ST8):** the primary path performs no clone, repository install, or repository import. Section 2 embeds the repository's six modules verbatim in topological dependency order (each tagged `metadata.dimer.embedded_module`; the only rewrites make `DEFAULT_WEIGHTS_DIR` working-directory-relative, replace the `resolve_weights_path` checkout-convenience line and remove internal relative package imports), Section 3 carries the 8-file snapshot manifest inline and asserts it against the module's constants before fetching, and `NOTEBOOK_SOURCE` records the repository, revision and module digests in the exports.
- **Parity (PAR1–PAR4):** `tests/test_notebook_parity.py` and `tools/validate_release_assets.py` fail when the carried modules, the inline manifest, or the inline pins differ from the repository, or when the notebook is not byte-identical to the generator's output.
- **Model acquisition (MOD1–MOD9, MOD12):** `stage_missing_files(WEIGHTS_DIR, allow_download=True)` fetches only the manifest entries a fresh runtime lacks, at the pinned revision (never `main`); `verify_snapshot` re-hashes every entry and asserts the pinned `model.safetensors` digest and byte count before the model is constructed; `Siglip2Pipeline.from_pretrained(weights_dir=WEIGHTS_DIR)` loads the verified files (`local_files_only=True`, `trust_remote_code=False`, `use_safetensors=True`) and picks CUDA when it is visible. No cell outside the carried modules calls `huggingface_hub`, `transformers`, `safetensors`, `urllib`, `torch.optim` or the model's towers directly.
- **Data (DAT1–DAT9, ST6):** the default sample is 360 research-grade, CC0-licensed iNaturalist photographs of six bird species (60 per species, one per observer), each pinned in the carried `samples.py` by photo id, byte size and SHA-256 and fetched from `https://inaturalist-open-data.s3.amazonaws.com/photos/<id>/medium.<ext>` (about 39 MB); a cached or fetched file is refused on any mismatch, and every record keeps its observation page and observer login. `build_sample_dataset(seed=SPLIT_SEED)` draws 36 / 8 / 16 per species (216 / 48 / 96) and `check_split_disjoint` asserts no photograph (by decoded-pixel digest) is shared. The template adds a second *External access* bullet naming the bucket, the byte count and the CC0 licence.
- **Stages (§4.1):** `fetch_corpus` + `read_corpus` + `build_sample_dataset` / `load_byod_dataset` + `split_dataset` (sample acquisition) → `validate_dataset` per split + `check_split_disjoint` + `observer_overlap` + refusal probes (validation, with three digests) → `validate_inputs` + `zero_shot_classify` / `retrieve` / `embed_image` with sanity checks + `evaluation_report` on the drawn shapes (inference contract) → `majority_baseline` + `colour_neighbour_baseline` + `pipe.evaluate` on the frozen model with the per-species breakdown and the scientific-name prompt set (baselines) → `pipe.adapt` (bounded fine-tuning) → `pipe.evaluate` on validation and test with the comparison (evaluation) → `zero_shot_classify` + `evaluation_report` on the shapes again (inference) → `pipe.save_artifact` → `Siglip2Pipeline.from_artifact` with the parity assertion (export and reload).
- **Score semantics (UNC1–UNC4):** sigmoid scores are independent, uncalibrated and prompt-dependent, and the classifier never abstains; accuracy, macro F1 and text-to-image mAP are computed from the model's own `logits_per_image` (own numpy implementations) and none is a human judgement; no acceptance threshold ships. The shapes re-scored after adaptation carry a `sample-sanity` verdict that names the missing dispersion.
- **Baselines and evidence (EVAL6/EVAL10/EVAL11):** the majority floor, the colour nearest neighbour and the frozen model are scored on the same 96 test photographs as the adapted model, on the three metrics and per species; every metric is labelled as one seeded split of one sample with no dispersion estimate, and the notebook says explicitly that the frozen model is already a usable zero-shot classifier, that the measured gain is concentrated on the retrieval view and the worst-separated species, and that a 48-photograph validation split moves accuracy in 2 % steps.
- **Split integrity (SPL1–SPL8):** the split is seeded and image-disjoint; validation (48 photographs) selects the epoch on text-to-image mAP and the test split is used for nothing but the final evaluation; observer overlap between splits is printed as an observation, and the notebook states that images from few photographers or sessions must be split by source.
- **Ceilings (VAL6):** `TEXT_MAX_LENGTH` (64), `DEFAULT_PROMPT_TEMPLATE`, `MAX_IMAGE_SIDE` (4096), the record bounds (8..20,000), the class bounds (2..100), the 64-character label ceiling and the id pattern are stated in the Prerequisites and printed before the model runs; four dataset refusals and one input refusal are demonstrated.
- **BYOD (DAT10–DAT19):** `USE_BYOD` defaults to `False` so the sample path never opens an upload dialog; when enabled, one zip is read member by member (no `extractall`) into the runtime and its `labels.csv` records flow through the same validation, image-disjoint split, baselines, fine-tuning, evaluation, export and reload cells. The expected schema and the privacy guidance are stated in the Prerequisites and in Section 4, and the training split is written to `outputs/siglip2_vision_language_train.csv` in the same shape.
- Licence: the weights are Apache-2.0 and the notebook, README and card say so; the photographs are CC0 1.0 and are not redistributed — the export records the corpus name, release, bucket, byte count and licence under `corpus`.
- `tools/validate_release_assets.py` performs source validation only. It does not satisfy the clean-runtime execution requirement; a release review must confirm that a recorded clean run in `../docs/release-verification.md` matches the notebook revision under review before the status is promoted to `Release-grade`.

## Outputs

The default notebook path writes (under `outputs/`):

- `siglip2_vision_language_train.csv` — the training split's labels table in the BYOD shape
- `siglip2_vision_language_input_manifest.json` — the inference-contract manifest (schema, ceilings, per-image observations, one recorded rejection finding)
- `siglip2_vision_language_evaluation_report.json` — baselines, frozen and adapted metrics on validation and test, the per-species comparison, the adaptation history
- `siglip2_vision_language_shapes.json` — the drawn shapes' rankings before and after adaptation
- `siglip2_vision_language_adapter/` — `adapter.safetensors` + `manifest.json`
- `siglip2_vision_language_result.json` — `NOTEBOOK_SOURCE`, model identity, snapshot, corpus, inference-contract reports, comparison, artifact digest, reload parity, runtime
- `provenance.json`

Earlier records for the superseded repository-installing and `MULTI-CAPABILITY` carriers are kept in `../docs/release-verification.md`.

## AI Assistance Disclosure

This repository’s code and accompanying documentation were developed with generative AI assistance for code development and technical writing under maintainer direction. The maintainer remains responsible for reviewing the implementation, validating results, and making release decisions. AI assistance does not constitute independent verification, provider endorsement, or release approval.


## Shared Vision-Language Retrieval learning unit — 2026-09-26

`DIMER_MultiModel_Vision_Language_Retrieval_Workshop.ipynb` is one learning unit with BLIP ITM and SigLIP2 repository entry points. Both carry identical executable cells. It remains **Candidate**; earlier primary notebook evidence does not qualify this supplemental three-model comparison.

The optional BYOD ZIP now runs actual SigLIP2, SigLIP v1 and BLIP ITC retrieval, BLIP ITM reranking, digest-verified embedding index export/reload and JSON/CSV results in an isolated run directory. STANDARD needs at least two evaluation images. FULL requires explicit train/validation/test roles with at least two images each; it adapts only on train, selects with validation, freezes before test and compares live adapted validation embeddings to a fresh adapter reload. Limits:128 images total,1–5 distinct string captions/image,256 characters/caption,16–4096px sides and100 million decoded pixels. Every image must be declared; duplicate decoded pixels are rejected within/across roles. ZIP extraction is nondestructive and rejects traversal, symlinks and duplicate members.

Runtime bootstrap retains an already imported NumPy2.x version and falls back to2.1.3, reflecting observed Colab2.1.3 preloads; other model/runtime pins are unchanged. Any incompatible preloaded packages still request a recorded session restart. This policy is tested locally, not a new uninterrupted hosted-run claim. ITM probabilities require exact cardinality and finite0–1 range; index reload checks digests and complete rankings in both directions.

The optional activity compares candidate-oracle recall at K5 versus10 on saved validation scores only. It does not require model reloads or overwrite canonical settings. Infrastructure cells are collapsed.
