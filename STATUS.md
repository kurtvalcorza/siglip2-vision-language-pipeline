# Release status

Current status: **Candidate — tutorial source complete, awaiting clean-runtime execution**. The pipeline package with its adaptation contract (`evaluate`, `adapt`, `save_artifact`, `from_artifact`), the digest-pinned iNaturalist sample and the metrics module, the offline unit tests, the model-backed regressions, `MODEL_CARD.md` (MODEL_CARD_SPEC 1.1), and the standalone `E2E` tutorial `tutorials/siglip2_vision_language_colab.ipynb` (generated by `tools/build_notebook.py`, DIMER Notebook Specification 2.0 §4) exist and pass static validation (`tools/validate_release_assets.py`, including the generator parity checks). The repository is not release-grade until clean-runtime execution evidence for the exact candidate commit is recorded in `docs/release-verification.md`. The earlier `MULTI-CAPABILITY` and repository-installing carriers' runs recorded there are superseded and do not count for the `E2E` blob.
